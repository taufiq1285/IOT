# Core dependencies untuk Dashboard
pandas>=2.2.0
numpy>=1.26.0
streamlit>=1.28.0
plotly>=5.18.0

# Parquet support (opsional, tapi recommended)
pyarrow>=15.0.0

# Untuk export/report (opsional, uncomment jika perlu)
 matplotlib>=3.9.0
 seaborn>=0.13.0
 openpyxl>=3.1.2
 tabulate>=0.9.0